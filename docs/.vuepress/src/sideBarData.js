/**
 * curryUri 需要导入的文件路径
 * sideBarInfo 
 *   title 二级标题名称，需要和pages下面的文件名一致
 *   folder 二级标题对应的文件名称
 */
const sideBarConfig = [
  {
    curryUri: '/handbook/base',
    sideBarInfo: [
      {
        title: 'css',
        folder: 'css',
      }
    ]
  }
]

module.exports = {
  sideBarConfig,
}
